# Reactive Vaadin + Spring Boot + LDAP

- Login with LDAP: users `user/password`, `admin/admin`
- Search customers reactively; add customers via form

## Run

Requires Java 17+ and Maven.

```bash
mvn spring-boot:run
```

App at http://localhost:8080
